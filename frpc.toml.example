[common]
server_addr = "公网IP地址"
server_port = 7000

# 反向代理 - 通过tcp协议，访问服务器的8000端口，反向代理到Django的8000端口
[[proxies]]
name = "想要使用的名称"
type = "协议的类型"
# Docker网络中使用服务名访问****容器
local_ip = "*****"
#想要暴露的端口
local_port = 8000
#映射到公网服务器上的端口
remote_port = 8000
